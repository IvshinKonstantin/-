Лабораторная работа №6: Интерфейсы и шаблоны ООП
📋 Описание задания
Задание 1: Кот
Цель: Реализация интерфейсов и полиморфизма на примере "мяукающих" объектов.

Задачи:
Кот мяукает:

Создать сущность Кот с полем "Имя"

Конструктор принимает имя кота

Метод ToString() возвращает "кот: Имя"

Метод Meow() выводит "Имя: мяу!"

Метод Meow(int n) выводит "мяу-" повторенное n раз

Создать кота "Барсик" и продемонстрировать мяуканье

Интерфейс Мяуканье:

Разработать интерфейс IMeowable с методом Meow()

Создать метод, принимающий набор объектов IMeowable

Продемонстрировать работу с разными типами "мяукающих" объектов

Количество мяуканий:

Добавить свойство MeowCount в интерфейс

Реализовать подсчет количества мяуканий

Продемонстрировать работу без изменения класса Кота

Задание 2: Дроби
Цель: Реализация шаблонов ООП на примере работы с дробями.

Задачи:
Дроби и операции:

Создать класс Дробь с числителем и знаменателем

Реализовать операции: сложение, вычитание, умножение, деление

Поддержка операций с целыми числами

Автоматическое упрощение дробей

Вычисление выражения: f1.sum(f2).div(f3).minus(5)

Сравнение дробей:

Переопределить методы Equals() и GetHashCode()

Дроби считаются равными, если их приведенные формы равны

Клонирование:

Реализовать интерфейс ICloneableFraction

Глубокое клонирование объектов

Кэширование:

Реализовать интерфейс IRealValueCache

Кэширование вещественного значения дроби

🏗️ Архитектура решения
Шаблон проекта
Оба задания реализованы по единому шаблону:

text
📁 Проект/
├── 📄 Интерфейсы/
│   ├── IMeowable.cs           # Интерфейс для мяукающих объектов
│   ├── ICloneableFraction.cs  # Интерфейс для клонирования
│   └── IRealValueCache.cs     # Интерфейс для кэширования
├── 📄 Модели/
│   ├── Cat.cs                 # Класс кота
│   ├── RobotCat.cs            # Другой мяукающий объект
│   └── Fraction.cs            # Класс дроби
├── 📄 Сервисы/
│   ├── MeowService.cs         # Сервис для работы с котами
│   └── FractionService.cs     # Сервис для работы с дробями
└── 📄 Program.cs              # Точка входа
Алгоритмы
Для Задания 1 (Коты):
pseudocode
1. Создать интерфейс IMeowable с методами:
   - Meow()
   - Свойство MeowCount

2. Реализовать класс Cat:
   - Поле Name (имя)
   - Поле _meowCount (счетчик)
   - Метод Meow() - увеличивает счетчик и выводит "мяу"
   - Метод Meow(n) - вызывает Meow() n раз

3. Реализовать сервисный класс Funs:
   - Метод MeowsCare(IMeowable[]) - вызывает Meow() у всех объектов
   - Метод MeowsCare(IMeowable) - вызывает Meow() 5 раз

4. Реализовать другие классы (RobotCat, ToyCat), 
   имплементирующие IMeowable
Для Задания 2 (Дроби):
pseudocode
1. Создать класс Fraction:
   - Поля _numerator, _denominator
   - Приватный метод Simplify() - упрощает дробь
   - Приватный метод GCD() - находит НОД

2. Реализовать арифметические операции:
   - Add(), Subtract(), Multiply(), Divide()
   - Перегрузка операторов +, -, *, /

3. Реализовать сравнение:
   - Переопределить Equals() и GetHashCode()
   - При сравнении приводить дроби к простейшей форме

4. Реализовать клонирование:
   - Интерфейс ICloneableFraction
   - Метод Clone() создает новый объект

5. Реализовать кэширование:
   - Интерфейс IRealValueCache
   - Поле _cachedRealValue для хранения вычисленного значения
   - Метод GetRealValue() использует кэш
🧪 Тестирование
Тест 1: Основная функциональность котов
csharp
// Создание и базовое мяуканье
Cat barsik = new Cat("Барсик");
barsik.Meow();        // Вывод: "Барсик: мяу!"
barsik.Meow(3);       // Вывод: "Барсик: мяу-мяу-мяу!"
Console.WriteLine(barsik.MeowCount); // Вывод: 4
Ожидаемый вывод:

text
Барсик: мяу!
Барсик: мяу-мяу-мяу!
4
Тест 2: Полиморфизм через интерфейс
csharp
IMeowable[] meowables = {
    new Cat("Мурзик"),
    new RobotCat("RC-3000"),
    new ToyCat("рыжий")
};

Funs.MeowsCare(meowables);
Ожидаемый вывод:

text
Мурзик: мяу!
RC-3000: бип-мяу!
рыжий игрушечный кот: пи-мяу!
Тест 3: Подсчет мяуканий
csharp
Cat testCat = new Cat("Тестовый");
Console.WriteLine($"До: {testCat.MeowCount}"); // 0
Funs.MeowsCare(testCat); // Мяукает 5 раз
Console.WriteLine($"После: {testCat.MeowCount}"); // 5
Ожидаемый вывод:

text
До: 0
Тестовый: мяу!
Тестовый: мяу!
Тестовый: мяу!
Тестовый: мяу!
Тестовый: мяу!
После: 5
Тест 4: Арифметика дробей
csharp
Fraction f1 = new Fraction(1, 3);
Fraction f2 = new Fraction(2, 3);

Console.WriteLine($"{f1} + {f2} = {f1 + f2}");   // 1/3 + 2/3 = 1/1
Console.WriteLine($"{f1} - {f2} = {f1 - f2}");   // 1/3 - 2/3 = -1/3
Console.WriteLine($"{f1} * {f2} = {f1 * f2}");   // 1/3 * 2/3 = 2/9
Console.WriteLine($"{f1} / {f2} = {f1 / f2}");   // 1/3 / 2/3 = 1/2
Ожидаемый вывод:

text
1/3 + 2/3 = 1/1
1/3 - 2/3 = -1/3
1/3 * 2/3 = 2/9
1/3 / 2/3 = 1/2
Тест 5: Сравнение дробей
csharp
Fraction f1 = new Fraction(2, 4);  // Упрощается до 1/2
Fraction f2 = new Fraction(1, 2);
Fraction f3 = new Fraction(3, 6);  // Упрощается до 1/2
Fraction f4 = new Fraction(2, 3);

Console.WriteLine($"f1 == f2: {f1 == f2}"); // True
Console.WriteLine($"f2 == f3: {f2 == f3}"); // True
Console.WriteLine($"f1 == f4: {f1 == f4}"); // False
Ожидаемый вывод:

text
f1 == f2: True
f2 == f3: True
f1 == f4: False
Тест 6: Клонирование
csharp
Fraction original = new Fraction(3, 7);
Fraction clone = (Fraction)original.Clone();

Console.WriteLine($"Оригинал: {original}"); // 3/7
Console.WriteLine($"Клон: {clone}");       // 3/7
Console.WriteLine($"Равны: {original == clone}"); // True
Console.WriteLine($"Та же ссылка: {ReferenceEquals(original, clone)}"); // False
Ожидаемый вывод:

text
Оригинал: 3/7
Клон: 3/7
Равны: True
Та же ссылка: False
Тест 7: Кэширование вещественного значения
csharp
Fraction f = new Fraction(1, 3);

// Первый вызов - вычисление
double v1 = f.GetRealValue(); // 0.333333...
Console.WriteLine($"Первое вычисление: {v1:F6}");

// Второй вызов - из кэша
double v2 = f.GetRealValue(); // 0.333333...
Console.WriteLine($"Второе вычисление: {v2:F6}");

// Изменяем дробь - кэш сбрасывается
f.Numerator = 2;
double v3 = f.GetRealValue(); // 0.666666...
Console.WriteLine($"После изменения: {v3:F6}");
Ожидаемый вывод:

text
Первое вычисление: 0.333333
Второе вычисление: 0.333333
После изменения: 0.666667
📊 Примеры работы (скриншоты)
Пример 1: Работа с котами
text
=== ЛАБОРАТОРНАЯ РАБОТА №6 - ЗАДАНИЕ 1: КОТЫ ===

======= ЗАДАНИЕ 1.1: Кот мяукает =======
Создан: кот: Барсик
Мяуканье 1 раз: Барсик: мяу!
Мяуканье 3 раза: Барсик: мяу-мяу-мяу!
Всего мяуканий: 4

======= ЗАДАНИЕ 1.2: Интерфейс Мяуканье =======
Вызываем мяуканье у всех объектов:
Барсик: мяу!
Мурзик: мяу!
Васька: мяу!
RC-3000: бип-мяу!
рыжий игрушечный кот: пи-мяу!
Пример 2: Работа с дробями
text
=== ЛАБОРАТОРНАЯ РАБОТА №6 - ЗАДАНИЕ 2: ДРОБИ ===

======= ЗАДАНИЕ 2.1: Дроби и операции =======
1/3 + 2/3 = 1/1
1/3 - 2/3 = -1/3
1/3 * 2/3 = 2/9
1/3 / 2/3 = 1/2

Вычисление выражения: f1.sum(f2).div(f3).minus(5)
f1 = 1/3, f2 = 2/3, f3 = 3/4
Результат: -16/9
Вещественное значение: -1.7778
Пример 3: Сравнение и клонирование
text
======= ЗАДАНИЕ 2.2: Сравнение дробей =======
2/4 == 1/2: True
1/2 == 3/6: True
2/4 == 2/3: False

======= ЗАДАНИЕ 2.3: Клонирование дроби =======
Оригинальная дробь: 3/7
Клонированная дробь: 3/7
original == cloned: True
ReferenceEquals(original, cloned): False
📝 Расписка шаблона
Структурные паттерны:
Интерфейс (Interface Pattern)

IMeowable - определяет контракт для мяукающих объектов

ICloneableFraction - определяет контракт для клонирования

IRealValueCache - определяет контракт для кэширования

Шаблонный метод (Template Method)

В классе Fraction методы операций следуют общей структуре

Автоматическое упрощение после каждой операции

Стратегия (Strategy Pattern)

Разные реализации IMeowable предоставляют разные стратегии мяуканья

Полиморфное поведение через интерфейс

Поведенческие паттерны:
Наблюдатель (Observer Pattern)

Сервисные классы наблюдают за изменениями в объектах

Автоматическое обновление кэша при изменении дроби

Прототип (Prototype Pattern)

Реализация интерфейса ICloneableFraction

Создание новых объектов через клонирование существующих

Принципы SOLID:
Single Responsibility Principle (SRP)

Класс Cat отвечает только за поведение кота

Класс Fraction отвечает только за математику дробей

Сервисные классы отвечают только за бизнес-логику

Open/Closed Principle (OCP)

Новые типы мяукающих объектов можно добавлять без изменения существующего кода

Расширение функциональности через интерфейсы

Liskov Substitution Principle (LSP)

Все классы, реализующие IMeowable, могут использоваться взаимозаменяемо

Все дроби ведут себя предсказуемо при операциях

Interface Segregation Principle (ISP)

Интерфейсы узконаправленные и специфичные

Классы реализуют только необходимые интерфейсы

Dependency Inversion Principle (DIP)

Высокоуровневые модули зависят от абстракций (интерфейсов)

Низкоуровневые модули реализуют эти абстракции

Шаблоны проектирования:
Фабричный метод (Factory Method)

Конструкторы классов выступают в роли фабрик

Создание объектов с валидацией параметров

Декоратор (Decorator Pattern)

Кэширование в классе Fraction действует как декоратор

Прозрачное добавление функциональности без изменения интерфейса

Компоновщик (Composite Pattern)

Работа с массивами объектов IMeowable как с единым целым

Единообразная обработка коллекций объектов

🎯 Оценка работы
Критерии оценивания:
Каждая задача: 3 балла × 7 задач = 21 балл

XML-документация: 2 балла

Использование шаблонов: +5 дополнительных баллов

Максимальный балл: 28 баллов

Распределение баллов:
Задание	Критерий	Баллы
1.1	Реализация класса Cat	3
1.2	Интерфейс IMeowable	3
1.3	Подсчет мяуканий	3
2.1	Арифметика дробей	3
2.2	Сравнение дробей	3
2.3	Клонирование	3
2.4	Кэширование	3
Документация	XML-комментарии	2
Шаблоны	Использование паттернов	+5
Итого максимум: 28 баллов

📦 Требования к запуску
Системные требования:
.NET 6.0 или выше

Visual Studio 2022 или VS Code

Windows/Linux/macOS

Инструкция по запуску:
Для задания 1 (Коты):

bash
cd CatAssignment
dotnet run
Для задания 2 (Дроби):

bash
cd FractionAssignment
dotnet run
Структура файлов в репозитории:
text
📁 Lab6_Solution/
├── 📁 CatAssignment/
│   └── 📄 CatAssignment.cs      # Полное решение задания 1
├── 📁 FractionAssignment/
│   └── 📄 FractionAssignment.cs # Полное решение задания 2
└── 📄 README.md                 # Эта документация
🔍 Дополнительные возможности
Возможные улучшения:
Для задания 1:

Добавить больше типов мяукающих объектов

Реализовать паттерн "Фабрика" для создания котов

Добавить логирование мяуканий

Для задания 2:

Реализовать операции с десятичными дробями

Добавить сериализацию/десериализацию

Реализовать паттерн "Строитель" для сложных выражений

Тестирование edge cases:
csharp
// Дробь с нулевым числителем
Fraction zero = new Fraction(0, 5); // Корректно: 0/1

// Отрицательный знаменатель
Fraction neg = new Fraction(3, -4); // Автоматически: -3/4

// Большие числа с автоматическим упрощением
Fraction big = new Fraction(1000000, 500000); // Упрощается до 2/1
📚 Использованные технологии и паттерны
Язык: C# 10.0

Платформа: .NET 6.0

Паттерны: Интерфейс, Шаблонный метод, Стратегия, Прототип

Принципы: SOLID, DRY, KISS

Документация: XML-комментарии

Архитектура: Многослойная (интерфейсы-модели-сервисы)

Автор: [Ваше имя]
Дата выполнения: [Дата]
Группа: [Номер группы]
Преподаватель: [ФИО преподавателя]
