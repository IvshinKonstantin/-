# 🚗 Система управления автопарком

## 📋 Описание задачи

Разработать консольное приложение для управления базой данных автопарка с возможностью:

1. 📂 Чтение данных из Excel файла (формат .xls)
2. 👁️ Просмотр всех данных (автомобили, водители, рейсы)
3. ❌ Удаление элементов по ключу
4. ➕ Добавление новых элементов
5. 🔍 Выполнение 4 типов запросов:
   - 1 запрос с обращением к одной таблице
   - 1 запрос с обращением к двум таблицам
   - 2 запроса с обращением к трем таблицам
   - 2 запроса возвращают перечень, 2 запроса - одно значение
6. 💾 Сохранение изменений в Excel файл

## 🏗️ Структура базы данных

### Таблица "Автомобили"
| Поле | Тип | Описание |
|------|-----|----------|
| ID автомобиля | Integer | Уникальный идентификатор |
| Марка | String | Марка автомобиля |
| Модель | String | Модель автомобиля |
| Год выпуска | Integer | Год выпуска |

### Таблица "Водители"
| Поле | Тип | Описание |
|------|-----|----------|
| ID водителя | Integer | Уникальный идентификатор |
| Имя | String | ФИО водителя |
| Возраст | Integer | Возраст водителя |
| Стаж вождения | Integer | Стаж вождения (лет) |

### Таблица "Рейсы"
| Поле | Тип | Описание |
|------|-----|----------|
| ID рейса | Integer | Уникальный идентификатор |
| ID автомобиля | Integer | Ссылка на автомобиль |
| ID водителя | Integer | Ссылка на водителя |
| Дата начала | DateTime | Дата и время начала рейса |
| Дата окончания | DateTime | Дата и время окончания рейса |
| Маршрут | String | Название маршрута |


## 🏗️ Архитектура программы

### Класс `Car` (Автомобиль)
```csharp
public class Car
{
    public int CarId { get; set; }
    public string Brand { get; set; }
    public string Model { get; set; }
    public int Year { get; set; }
    
    public Car(int carId, string brand, string model, int year) { ... }
    public override string ToString() { ... }
}
```
Класс Driver (Водитель)
```csharp
public class Driver
{
    public int DriverId { get; set; }
    public string Name { get; set; }
    public int Age { get; set; }
    public int Experience { get; set; }
    
    public Driver(int driverId, string name, int age, int experience) { ... }
    public override string ToString() { ... }
}
```
Класс Trip (Рейс)
```csharp
public class Trip
{
    public int TripId { get; set; }
    public int CarId { get; set; }
    public int DriverId { get; set; }
    public DateTime StartDate { get; set; }
    public DateTime EndDate { get; set; }
    
    public Trip(int tripId, int carId, int driverId, DateTime startDate, DateTime endDate, string route) { ... }
    public override string ToString() { ... }
}
```
Класс DatabaseManager (Менеджер БД)
```csharp
public class DatabaseManager
{
    private List<Car> cars;
    private List<Driver> drivers;
    private List<Trip> trips;
    private string filePath;
    
    // Основные методы:
    public void LoadData() { ... }
    public void ViewCars() { ... }
    public void DeleteCar(int carId) { ... }
    public void AddCar() { ... }
    public List<Car> GetCarsByBrand(string brand) { ... }
    public void SaveData(string outPath) { ... }
}
```
🔄 Алгоритм работы программы
```graph TD
Основной алгоритм работы программы:

1. Начало
   ├─ Запуск программы
   └─ Загрузка данных из файла Excel

2. Основной цикл
   ├─ Отображение главного меню
   ├─ Ожидание выбора пользователя
   └─ Выполнение выбранного действия

3. Доступные действия:
   ├─ 1: Просмотр автомобилей
   ├─ 2: Удаление элемента
   ├─ 3: Добавление элемента
   ├─ 4: Выполнение запросов
   ├─ 5: Сохранение данных
   └─ 6: Выход из программы

4. После выполнения действия (кроме выхода):
   └─ Возврат к п.2 (главное меню)
```
🔍 Реализованные запросы
📊 1. Запрос к одной таблице
Формулировка: "Получить список всех автомобилей определенной марки"
```csharp
public List<Car> GetCarsByBrand(string brand)
{
    return cars.Where(c => c.Brand.Equals(brand, StringComparison.OrdinalIgnoreCase)).ToList();
}
```
👨‍✈️ 2. Запрос к двум таблицам
Формулировка: "Получить список водителей со стажем более указанного значения"
```csharp
public List<dynamic> GetTripsWithCarInfo()
{
    var query = from trip in trips
                join car in cars on trip.CarId equals car.CarId
                select new
                {
                    TripId = trip.TripId,
                    CarBrand = car.Brand,
                    CarModel = car.Model,
                    StartDate = trip.StartDate,
                    EndDate = trip.EndDate,
                    Route = trip.Route
                };
    
    return query.ToList<dynamic>();
}
```
📈 3. Запрос к трем таблицам (возвращает перечень)
Формулировка: "Получить полную информацию о всех рейсах"
```csharp
public List<dynamic> GetFullTripInfo()
{
    var query = from trip in trips
                join car in cars on trip.CarId equals car.CarId
                join driver in drivers on trip.DriverId equals driver.DriverId
                select new
                {
                    TripId = trip.TripId,
                    CarInfo = $"{car.Brand} {car.Model} ({car.Year})",
                    DriverInfo = $"{driver.Name} (Стаж: {driver.Experience} лет)",
                    StartDate = trip.StartDate.ToString("dd.MM.yyyy HH:mm"),
                    EndDate = trip.EndDate.ToString("dd.MM.yyyy HH:mm"),
                    Route = trip.Route,
                    DurationHours = (trip.EndDate - trip.StartDate).TotalHours
                };
    
    return query.ToList<dynamic>();
}
```
📊 4. Запрос к трем таблицам (возвращает число)
Формулировка: "Подсчитать количество рейсов в 2023 году, выполненных водителями старше 40 лет на автомобилях моложе 10 лет"

```csharp
public int GetCountOfTripsByOlderDriversOnNewCars()
{
    var currentYear = DateTime.Now.Year;
    
    var query = (from trip in trips
                 where trip.StartDate.Year == 2023
                 join car in cars on trip.CarId equals car.CarId
                 join driver in drivers on trip.DriverId equals driver.DriverId
                 where driver.Age > 40 && (currentYear - car.Year) < 10
                 select trip).Count();
    
    return query;
}
```
🎯 Пример запроса из задания
Формулировка: "Определить количество рейсов, совершенных в 2023 году на автомобилях марки Toyota, выпущенных после 2005 года"
```csharp
public int GetExampleQueryCount()
{
    var query = (from trip in trips
                where trip.StartDate.Year == 2023 && trip.EndDate.Year == 2023
                join car in cars on trip.CarId equals car.CarId
                where car.Brand.Equals("Toyota", StringComparison.OrdinalIgnoreCase) 
                      && car.Year > 2005
                select trip).Count();
    
    return query;
}
```
🧪 Тестирование программы
Тест 1: Загрузка данных ✅
Цель: Проверить корректность загрузки данных из Excel файла

Шаги:

Запустить программу

Программа автоматически загружает данные из LR5-var4.xlsx

Проверить вывод статистики

Ожидаемый результат:

```text
Загружено 1000 автомобилей
Загружено 438 водителей
Создано 100 тестовых рейсов
```
Тест 2: Просмотр данных ✅
Цель: Проверить возможность просмотра всех таблиц

Шаги:

В главном меню выбрать "1. Просмотр данных"

Выбрать "1. Просмотреть все автомобили"

Вернуться в меню и выбрать "2. Просмотреть всех водителей"

Вернуться в меню и выбрать "3. Просмотреть все рейсы"

Ожидаемый результат: Корректный вывод всех записей в читаемом формате

Тест 3: Выполнение запросов ✅
Цель: Проверить корректность работы всех запросов

Шаги:

В главном меню выбрать "4. Выполнение запросов"

Выполнить поочередно все 4 запроса

Проверить правильность результатов

Ожидаемый результат: Запросы выполняются без ошибок, возвращают корректные данные

🚀 Особенности реализации
| Особенность | Описание |
|-------------|----------|
| **Использование LINQ** | Все запросы реализованы с помощью LINQ-выражений |
| **Обработка ошибок** | Все критические операции защищены try-catch блоками |
| **Валидация данных** | Проверка корректности вводимых пользователем данных |
| **Постраничный вывод** | Для больших списков реализован постраничный вывод |
| **Связность данных** | При удалении автомобиля или водителя автоматически удаляются связанные рейсы |
| **Сохранение состояния** | Все изменения можно сохранить обратно в Excel файл |
| EPPLus | Используется стабильная версия без требований к лицензии |
